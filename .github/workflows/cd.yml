name: CD

on:
  release:
    types:
      - prereleased
      - released

jobs:
  milestone:
    if: startsWith(github.ref, 'refs/tags')
    uses: ComPWA/actions/.github/workflows/close-milestone.yml@244cbdc9ef919cf659fcf26abeb04d6da39434ac # 4.1.1
  package-name:
    uses: ComPWA/actions/.github/workflows/get-pypi-name.yml@244cbdc9ef919cf659fcf26abeb04d6da39434ac # 4.1.1
  pypi:
    environment:
      name: PyPI
      url: https://pypi.org/p/${{ needs.package-name.outputs.name }}
    if: startsWith(github.ref, 'refs/tags')
    name: Publish to PyPI
    needs:
      - package-name
    permissions:
      id-token: write
    runs-on: ubuntu-24.04
    steps:
      - uses: ComPWA/actions/build-pypi-distribution@244cbdc9ef919cf659fcf26abeb04d6da39434ac # 4.1.1
      - uses: pypa/gh-action-pypi-publish@dc37677b2e1c63e2034f94d8a5b11f265b73ba33 # v1.14.2
  push:
    if: startsWith(github.ref, 'refs/tags') && !github.event.release.prerelease
    secrets: inherit
    uses: ComPWA/actions/.github/workflows/push-to-version-branches.yml@244cbdc9ef919cf659fcf26abeb04d6da39434ac # 4.1.1
